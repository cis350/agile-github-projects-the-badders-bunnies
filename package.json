{
  "name": "my-fullstack-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "turbo format"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "turbo": "^1.11.2"
  },
  "devDependencies": {
    "@types/cookie-session": "^2.0.48"
  }
}
